#!/usr/bin/env node

const
	path = require('path'),
	fs = require('fs')

// TODO: should not be necessary
require('mason/dist/private/boot-order')._get.get()

const dist = path.join(path.dirname(fs.realpathSync(__filename)), '../dist')
require(dist).default()
